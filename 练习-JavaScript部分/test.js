function getMethods(m,n){
    let dp=new Array(m);
    for(let i=0;i<m;i++){
        dp[i]=new Array(n).fill(0);
    }
    for(let i=1;i<m;i++){
        for(let j=1;j<n;j++){
            
        }
    }
}
getMethods(4,2);